1. Project Setup (React + Tailwind)

Create React App with Tailwind CSS
Folder structure setup

2. Routing & Layout

React Router setup (react-router-dom)
Build a base layout (Header, Footer, etc.)
Responsive, polished navigation bar

3. Authentication Pages

Login Page (session-based)
Register Page
User details edit
Auth Context with session management -- ??

4. Event Pages

Home (list all events)
Event Details
Create Event  
Edit Event 
Delete Event 
My Created Events 
RSVP’d Events  
Show users RSVPed to my events   

5. Upload Banner UI

File upload input in event creation
Display uploaded image preview   

6. Admin Dashboard

Show stats (from /admin/stats)
Only accessible to Admins   


7. Features UI

Search + Filters    
CSV Export button               -------------------- POINT
Calendar view (for events)
Map view (location-based)


Notes future improvment:

 - admin ban user 
 - get events based on people interests
 - creator report page
 - QR Code view for check-ins 
 - give eventname in csv files name
 - end time for events

Problem

 -  if i click the button twice fast im getting rsvp twice and slots also occupied twice



Login.jsx
  <div className="min-h-screen flex items-center justify-center bg-gray-100 p-6">
      <form
        onSubmit={handleSubmit}
        className="w-full max-w-md bg-white rounded-2xl shadow-xl p-8"
      >
        <h2 className="text-2xl font-semibold text-center mb-6">Login</h2>

        <div className="mb-4">
          <label className="block text-sm font-medium mb-1">Email</label>
          <input
            type="email"
            name="email"
            value={formData.email}
            onChange={handleChange}
            className="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
            placeholder="Enter your email"
            autoComplete="username"
          />
        </div>

        <div className="mb-6">
          <label className="block text-sm font-medium mb-1">Password</label>
          <input
            type="password"
            name="password"
            value={formData.password}
            onChange={handleChange}
            className="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
            placeholder="Enter your password"
            autoComplete="current-password"
          />
        </div>

        <button
          type="submit"
          disabled={loading}
          className="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-lg transition duration-200 disabled:opacity-60"
        >
          {loading ? "Logging in..." : "Login"}
        </button>
        <div className="mt-4 text-center">
          <p className="text-sm text-gray-600">
          Don't have an account?{" "}
          <a href="/register" className="text-blue-600 hover:underline">
            Register here
          </a>
  </p>
</div>

      </form>
    </div>





